[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "validkit"
version = "0.1.0"
description = "Neun kleine, reine Prüf- und Normalisierungsfunktionen, nur mit der Standardbibliothek."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["validkit*"]
